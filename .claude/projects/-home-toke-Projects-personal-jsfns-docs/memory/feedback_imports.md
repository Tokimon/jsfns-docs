---
name: JS extensions in TS imports
description: Import paths use .js extensions even when the source files are .ts — standard TypeScript convention
type: feedback
---

When importing .ts files, always use .js extensions in the import path. Do not change import extensions from .js to .ts.
